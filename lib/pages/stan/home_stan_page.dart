import 'package:flutter/material.dart';

class HomeStanPage extends StatelessWidget {
  const HomeStanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Home Stan"),
    );
  }
}
