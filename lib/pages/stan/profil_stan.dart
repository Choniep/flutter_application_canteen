import 'package:flutter/material.dart';

class ProfilStan extends StatelessWidget {
  const ProfilStan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Profil Stan"),
      ),
    );
  }
}
